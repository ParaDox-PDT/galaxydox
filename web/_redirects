# /admin exact path upstream'da /admin/login ga o'tadi.
# Uni 200 proxy qilish redirect loop beradi, shuning uchun avval real redirect qilamiz.
/admin    /admin/login                                  302!
/admin/*  https://galaxydoxadmin.netlify.app/admin/:splat  200

# .well-known dotfolder Netlify manual deploy'da yo'qoladi.
# Fayllar well-known/ (noktasiz) papkasida saqlanadi va shu redirect orqali serve qilinadi.
/.well-known/*  /well-known/:splat  200

# Asosiy Flutter Web loyihasi uchun yo'naltirish
/* /index.html                                      200
